apiVersion: v1
kind: Service
metadata:
  name: aceest-fitness-service
  namespace: aceest
spec:
  selector:
    app: aceest-fitness
    version: stable
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
